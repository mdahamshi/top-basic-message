<h1>
  Welcome to the
  <%= appName %>
</h1>

<ul class="chat-container">
  <% messages.sort((a, b) => b.added - a.added).forEach((msg) => { %>
  <%- include('component/message', { msg: msg }) %>
  <% }); %>
</ul>
